name: DeepInteractome CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Install lightweight subset for CI (skip heavy torch/torchvision)
        pip install numpy pandas scikit-learn scipy requests tqdm pyyaml \
                    click python-dotenv biopython joblib \
                    fastapi uvicorn httpx pytest

    - name: Run tests
      run: |
        pytest tests/ -v --tb=short
